# 共享账号池

新增一个独立的共享账号池：用户上传自己的平台账号和代理，系统自动检测后立即上线；其他用户通过专用共享分组调用。每次成功扣费后按平台抽成比例给账号提供者记收益，收益经过冻结期后可转入提供者的平台余额。共享账号使用卡片展示，并显示最近调用请求和调用次数。

共享账号必须和原始管理员账号池隔离。普通调度器排除 `account_scope=shared`，共享调度器只选择 active listing。凭证永不返回前端，代理和凭证按用户权限加密保存。
